[Unit]
Description=Selenium Standalone Server
After=network.target network-online.target

[Service]
ExecStart=<%= @java %> -jar <%= @install_root %>/jars/<%= @jar_name %> <%= @options %> 
KillMode=process
User=<%= @user %>
Environment=DISPLAY=<%= @display %>
[Install]
WantedBy=multi-user.target
